substitutions:
  _IMAGE: "europe-west6-docker.pkg.dev/clever-courier-474119-r9/mn-transcribe/worker:latest"

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile.worker', '-t', '$_IMAGE', '.']

images:
  - '$_IMAGE'
